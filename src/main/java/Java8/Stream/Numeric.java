package Java8.Stream;

import java.util.stream.IntStream;

public class Numeric {

	public void genaerateRangeClosed() {
		/*
		 * IntStream.rangeClosed(1, 100).filter(b -> Math.sqrt(a * a + b * b) % 1 ==
		 * 0).boxed() .map(b -> new int[] { a, b, (int) Math.sqrt(a * a + b * b) });
		 */
	}
}
